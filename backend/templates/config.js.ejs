appLocale = localStorage.getItem('LOCALE') || navigator.language || window.LOCALE || 'pl';
require = {
  baseUrl: '/',
  paths: <%- paths %>,
  shim: <%- shim %>,
  config: {
    i18n: {
      locale: appLocale
    }
  }
};
