// Part of <https://miracle.systems/p/walkner-wmes> licensed under <CC BY-NC-SA 4.0>

'use strict';

const path = require('path');

module.exports = (app, module, req, res) =>
{
  res.sendFile(path.resolve(module.config.nc12ToCagsJsonPath));
};
