// Part of <https://miracle.systems/p/walkner-wmes> licensed under <CC BY-NC-SA 4.0>

'use strict';

module.exports = (app, module) =>
{
  const {
    messengerServer
  } = module;

  messengerServer.handle('ct.getAllLineState', handleGetAllLineState);

  function handleGetAllLineState(req, done)
  {
    module.getAllLineState(done);
  }
};
