// Part of <https://miracle.systems/p/walkner-wmes> licensed under <CC BY-NC-SA 4.0>

'use strict';

module.exports = (app, module) =>
{
  const {
    express,
    mongoose,
    user
  } = module;

  const canView = user.auth('LOCAL');

  express.get('/inveo/:line/:station', canView, (req, res) =>
  {
    console.log(Date.now(), req.query.mac, req.query.id);

    res.sendStatus(204);
  });
};
