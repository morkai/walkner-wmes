<h3 style="font-family: Arial, sans-serif; font-size: 15px; border-bottom: 1px solid #e5e5e5; margin-top: 0; margin-bottom: 3px; padding-bottom: 3px;">
  Narzędzie pomiarowe
</h3>
<table style="font-family: Arial, sans-serif; font-size: 12px">
  <tbody>
  <tr>
    <th align="left" style="white-space: nowrap">ID</th>
    <td>: <a href="<%- urlPrefix %>r/toolcal/<%- tool.rid %>"><%- tool.rid %></a></td>
  </tr>
  <tr>
    <th align="left" style="white-space: nowrap">Typ urządzenia</th>
    <td>: <%= tool.type %></td>
  </tr>
  <tr>
    <th align="left" style="white-space: nowrap">Nazwa urządzenia</th>
    <td>: <%= tool.name %></td>
  </tr>
  <% if (tool.sn) { %>
  <tr>
    <th align="left" style="white-space: nowrap">Nr seryjny</th>
    <td>: <%= tool.sn %></td>
  </tr>
  <% } %>
  <tr>
    <th align="left" style="white-space: nowrap">Data ostatniej kalibracji</th>
    <td>: <%= tool.lastDate %></td>
  </tr>
  <tr>
    <th align="left" style="white-space: nowrap">Okres kalibracji</th>
    <td>: <%= tool.interval %></td>
  </tr>
  <tr>
    <th align="left" style="white-space: nowrap">Data następnej kalibracji</th>
    <td>: <%= tool.nextDate %></td>
  </tr>
  <tr>
    <th align="left" style="white-space: nowrap">Dysponenci sprzętu</th>
    <td>
      <% if (tool.users.length === 0) { %>
      :
      <% } else if (tool.users.length === 1) { %>
      : <%= tool.users[0].label %>
      <% } else { %>
      <ul style="margin: 0; padding: 0; list-style-position: inside">
        <% tool.users.forEach((user) => { %>
        <li><%= user.label %></li>
        <% }) %>
      </ul>
      <% } %>
    </td>
  </tr>
</table>
<h3 style="font-family: Arial, sans-serif; font-size: 15px; border-bottom: 1px solid #e5e5e5; margin-bottom: 3px; padding-bottom: 3px;">
  Na skróty
</h3>
<ul style="margin-top: 0; font-family: Arial, sans-serif; font-size: 12px">
  <li><a href="<%- urlPrefix %>r/toolcal/<%- tool.rid %>">Narzędzie pomiarowe</a></li>
  <li><a href="<%- urlPrefix %>r/toolcal/mine">Twoje narzędzia pomiarowe</a></li>
  <li><a href="<%- urlPrefix %>r/toolcal/all">Wszystkie narzędzia pomiarowe</a></li>
</ul>
<hr>
<font face="Arial" color="Gray" size="1">
  Ta wiadomość została wygenerowana automatycznie przez system WMES ponieważ jesteś przypisany do danego narzędzia
  pomiarowego i zbliża się jego data kalibracji.
</font>
