// Part of <https://miracle.systems/p/walkner-wmes> licensed under <CC BY-NC-SA 4.0>

'use strict';

const getComponentLabels = require('./getComponentLabels');

exports.DEFAULT_CONFIG = {
  mongooseId: 'mongoose',
  expressId: 'express',
  userId: 'user',
  html2pdfId: 'html2pdf'
};

exports.recordTopics = {
  blacklist: [
    'orderBomMatchers.added', 'orderBomMatchers.edited',
    'componentLabels.added', 'componentLabels.edited'
  ]
};

exports.models = [
  require('./models/orderBomMatcher'),
  require('./models/componentLabel'),
  require('./models/componentLabelCounter'),
  require('orders/models/order'),
  require('prodLines/models/prodLine')
];

exports.optionalModules = {
  'mongoose user express html2pdf': require('./routes')
};

exports.start = (app, module) =>
{
  module.getComponentLabels = getComponentLabels.bind(null, app, module);
};
