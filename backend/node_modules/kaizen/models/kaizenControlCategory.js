// Part of <https://miracle.systems/p/walkner-wmes> licensed under <CC BY-NC-SA 4.0>

'use strict';

exports.name = 'KaizenControlCategory';

exports.setUp = (app, mongoose) =>
{
  const schema = new mongoose.Schema({
    active: {
      type: Boolean,
      default: true
    },
    shortName: {
      type: String,
      required: true,
      trim: true
    },
    fullName: {
      type: String,
      required: true,
      trim: true
    }
  }, {
    id: false
  });

  schema.statics.TOPIC_PREFIX = 'kaizen.controlCategories';
  schema.statics.BROWSE_LIMIT = 1000;

  return schema;
};
