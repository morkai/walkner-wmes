<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>hd</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Verdana, sans-serif;
      line-height: 1;
      text-align: center;
      text-rendering: geometricPrecision;
    }
    #container {
      margin: 0 auto;
      width: 718px;
      text-align: left;
    }
    table {
      width: 100%;
      border-bottom: 2px solid #3C3C41;
      border-collapse: collapse;
    }
    td {
      vertical-align: bottom;
      padding-bottom: 10px;
    }
    #right {
      width: 150px;
    }
    #title {
      display: block;
      font-size: 18px;
      font-weight: bold;
    }
    #date {
      font-size: 13px;
      font-weight: normal;
    }
    #company {
      display: block;
      margin-top: 5px;
      font-size: 13px;
      font-weight: normal;
    }
    .omrMarker {
      width: 1px;
    }
    .omrMarker span {
      display: inline-block;
      width: 30px;
      height: 30px;
      background: #000;
    }
    .omrMarker:first-child {
      padding-right: 10px;
    }
  </style>
</head>
<body>
<div id="container">
  <table>
    <tr>
      <td class="omrMarker"><span></span></td>
      <td id="left">
        <span id="title"><%- LANG.HEADER %>, <span id="date"><%- moment(survey.startDate).format('D MMMM YYYY') %>-<%- moment(survey.endDate).format('D MMMM YYYY') %></span></span>
        <span id="company"><%- LANG.COMPANY %></span>
      </td>
      <td id="right"></td>
      <td class="omrMarker"><span></span></td>
    </tr>
  </table>
</div>
</body>
</html>
