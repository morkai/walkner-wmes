// Part of <https://miracle.systems/p/walkner-wmes> licensed under <CC BY-NC-SA 4.0>

'use strict';

exports.name = 'D8EntrySource';

exports.setUp = (app, mongoose) =>
{
  const d8EntrySourceSchema = new mongoose.Schema({
    _id: {
      type: String,
      required: true,
      match: /^[A-Za-z0-9-_]+$/
    },
    name: {
      type: String,
      required: true,
      trim: true
    },
    position: {
      type: Number,
      default: 0
    }
  }, {
    id: false
  });

  d8EntrySourceSchema.statics.TOPIC_PREFIX = 'd8.entrySources';
  d8EntrySourceSchema.statics.BROWSE_LIMIT = 1000;

  mongoose.model(exports.name, d8EntrySourceSchema);
};
