<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%- order %> - Konstrukcja na zamówienie - ETO</title>
  <style>
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0 15px;
      padding-top: <%- header ? 33 : 0 %>px;
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      font-size: 18px;
      line-height: 1;
      color: #fff;
      background: #525659;
      cursor: default;
    }
    h1 {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      margin: 0;
      padding: 7px 8px 8px 8px;
      height: 33px;
      overflow: hidden;
      white-space: nowrap;
      font-family: Verdana, sans-serif;
      font-size: 18px;
      font-weight: bold;
      text-transform: uppercase;
      text-shadow: 1px 1px 1px #333;
      text-overflow: ellipsis;
      text-align: center;
      color: #fff;
      background-color: #323639;
      background-image: linear-gradient(#323639, rgba(50, 54, 57, 0.95));
      border-bottom: 1px solid #000;
    }
    body > table {
      margin: 15px auto;
      width: auto;
      background: #fff;
      color: #333;
      border-collapse: collapse;
      box-shadow: 0 4px 12px #333;
    }
    body > p {
      margin: 15px;
      padding: 15px;
      background: #fff;
      color: #333;
      box-shadow: 0 4px 12px #333;
    }
    table * {
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif!important;
      font-size: 18px!important;
      height: auto!important;
      width: auto!important;
      vertical-align: top!important;
    }
    table p {
      margin: 0!important;
      padding: 0!important;
    }
    table td {
      padding: 5px!important;
      line-height: 1.4!important;
    }
  </style>
</head>
<body>
<% if (header) { %>
<h1 id="hd">Konstrukcja na zamówienie</h1>
<% } %>
<% if (!etoTableHtml) { %>
<p>Nie znaleziono ETO dla 12NC <%- nc12 %>.</p>
<% } else { %>
<%- etoTableHtml %>
<% } %>
<script>
  window.addEventListener('contextmenu', function(e)
  {
    e.preventDefault();
  });

  window.addEventListener('touchstart', function(e)
  {
    if (e.touches && e.touches.length > 1)
    {
      e.preventDefault();
    }
  }, {passive: false});
</script>
</body>
</html>
