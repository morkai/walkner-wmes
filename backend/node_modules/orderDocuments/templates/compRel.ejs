<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%- order %> - Dopuszczenia</title>
  <style>
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      padding-top: 33px;
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      font-size: 14px;
      line-height: 1;
      color: #fff;
      background: #525659;
      cursor: default;
    }
    h1 {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      margin: 0;
      padding: 7px 8px 8px 8px;
      height: 33px;
      overflow: hidden;
      white-space: nowrap;
      font-family: Verdana, sans-serif;
      font-size: 18px;
      font-weight: bold;
      text-transform: uppercase;
      text-shadow: 1px 1px 1px #333;
      text-overflow: ellipsis;
      text-align: center;
      color: #fff;
      background-color: #323639;
      background-image: linear-gradient(#323639, rgba(50, 54, 57, 0.95));
      border-bottom: 1px solid #000;
    }
    p {
      margin: 15px;
      padding: 15px;
      background: #fff;
      color: #333;
      box-shadow: 0 4px 12px #333;
    }
    #compRels {
      padding: 0 0 15px 15px;
      font-size: 42px;
      line-height: 1;
    }
    .compRel {
      display: flex;
      margin: 15px 15px 0 0;
    }
    .compRel-old,
    .compRel-new {
      flex: 1 1 auto;
      width: 50%;
    }
    .compRel-separator {
      margin: 0 15px;
      flex: 0 0 auto;
      font-size: 70px;
      line-height: 72px;
    }
    .compRel-code {
      color: #E00;
    }
    .compRel-new .compRel-code {
      color: #5cb85c;
    }
    .compRel-name {
      display: block;
      margin-top: 2px;
      font-size: 28px;
    }
  </style>
</head>
<body>
<h1 id="hd">Lista dopuszczeń komponentów</h1>
<% if (!compRels.length) { %>
<p>Zlecenie <%- order %> nie ma żadnych dopuszczeń.</p>
<% } else { %>
<div id="compRels">
  <% compRels.forEach(compRel => { %>
  <p class="compRel">
    <span class="compRel-old">
      <span class="compRel-code"><%- compRel.oldCode %></span>
      <span class="compRel-name"><%= compRel.oldName %></span>
    </span>
    <span class="compRel-separator">➡</span>
    <span class="compRel-new">
      <span class="compRel-code"><%- compRel.newCode %></span>
      <span class="compRel-name"><%= compRel.newName %></span>
    </span>
  </p>
  <% }) %>
</div>
<% } %>
<script>
  'use strict';

  window.addEventListener('contextmenu', function(e)
  {
    e.preventDefault();
  });

  window.addEventListener('touchstart', function(e)
  {
    if (e.touches && e.touches.length > 1)
    {
      e.preventDefault();
    }
  }, {passive: false});
</script>
</body>
</html>
