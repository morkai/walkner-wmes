// Part of <https://miracle.systems/p/walkner-wmes> licensed under <CC BY-NC-SA 4.0>

'use strict';

exports.name = 'CtOrderGroup';

exports.setUp = (app, mongoose) =>
{
  const schema = new mongoose.Schema({
    active: {
      type: Boolean,
      required: true
    },
    name: {
      type: String,
      required: true,
      trim: true
    },
    nameInclude: [[String]],
    nameExclude: [[String]],
    nc12Include: [String],
    nc12Exclude: [String],
    bomInclude: [[String]],
    bomExclude: [[String]]
  }, {
    id: false,
    minimize: false
  });

  schema.statics.TOPIC_PREFIX = 'ct.orderGroups';
  schema.statics.BROWSE_LIMIT = 100;

  return schema;
};
