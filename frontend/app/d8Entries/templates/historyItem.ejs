<div class="d8Entries-history-item <%- item.changes.length ? 'has-changes' : '' %> <%- item.comment !== '' ? 'has-comment' : '' %> <%- item.seen ? '' : 'is-unseen' %>">
  <span class="d8Entries-history-user"><%- item.user %></span>,
  <time class="d8Entries-history-time" datetime="<%- item.time.iso %>" title="<%- item.time.long %>"><%- item.time.daysAgo > 5 ? item.time.long : item.time.human %></time>:
  <% if (item.changes.length) { %>
  <table class="table table-condensed d8Entries-history-changes">
    <tbody>
      <% item.changes.forEach(function(change) { %>
      <tr>
        <td class="is-min"><%- change.label %>:</td>
        <td class="is-min"><span class="<%- change.oldValue.more ? 'has-more' : '' %>" data-content="<%= change.oldValue.more %>"><%- change.oldValue %></span></td>
        <td class="is-min"><i class="fa fa-arrow-right"></i></td>
        <td><span class="<%- change.newValue.more ? 'has-more' : '' %>" data-content="<%= change.newValue.more %>"><%- change.newValue %></span></td>
        <td></td>
      </tr>
      <% }) %>
    </tbody>
  </table>
  <% } %>
  <% if (item.comment !== '') { %>
  <p class="d8Entries-history-comment"><%= item.comment %></p>
  <% } %>
</div>
