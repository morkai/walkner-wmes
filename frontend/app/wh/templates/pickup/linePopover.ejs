<table class="wh-list-popover-line" data-order-id="<%- orderId %>">
  <tbody>
  <tr>
    <td colspan="2"></td>
    <% lines.forEach(function(line) { %>
    <td class="text-center">
      <%= line._id %>
      <% if (line.redirLine) { %>
      <br>
      <i class="fa fa-arrow-down"></i>
      <br>
      <%= line.redirLine %>
      <% } %>
    </td>
    <% }) %>
  </tr>
  <tr>
    <td rowspan="3"><%- t('wh-lines', 'PROPERTY:pickup:started') %></td>
    <td><%- t('wh-lines', 'PROPERTY:sets') %></td>
    <% lines.forEach(function(line) { %>
      <td class="text-center"><%- line.pickup.started.sets.toLocaleString() %></td>
    <% }) %>
  </tr>
  <tr>
    <td><%- t('wh-lines', 'PROPERTY:qty') %></td>
    <% lines.forEach(function(line) { %>
    <td class="text-center"><%- line.pickup.started.qty.toLocaleString() %></td>
    <% }) %>
  </tr>
  <tr>
    <td><%- t('wh-lines', 'PROPERTY:time') %></td>
    <% lines.forEach(function(line) { %>
    <td class="text-center"><%- time.toString(line.pickup.started.time / 1000, true, false) %></td>
    <% }) %>
  </tr>
  <tr>
    <td rowspan="3"><%- t('wh-lines', 'PROPERTY:pickup:finished') %></td>
    <td><%- t('wh-lines', 'PROPERTY:sets') %></td>
    <% lines.forEach(function(line) { %>
      <td class="text-center"><%- line.pickup.finished.sets.toLocaleString() %></td>
    <% }) %>
  </tr>
  <tr>
    <td><%- t('wh-lines', 'PROPERTY:qty') %></td>
    <% lines.forEach(function(line) { %>
    <td class="text-center"><%- line.pickup.finished.qty.toLocaleString() %></td>
    <% }) %>
  </tr>
  <tr>
    <td><%- t('wh-lines', 'PROPERTY:time') %></td>
    <% lines.forEach(function(line) { %>
    <td class="text-center"><%- time.toString(line.pickup.finished.time / 1000, true, false) %></td>
    <% }) %>
  </tr>
  <tr>
    <td rowspan="2"><%- t('wh-lines', 'PROPERTY:available') %></td>
    <td><%- t('wh-lines', 'PROPERTY:qty') %></td>
    <% lines.forEach(function(line) { %>
    <td class="text-center"><%- line.available.qty.toLocaleString() %></td>
    <% }) %>
  </tr>
  <tr>
    <td><%- t('wh-lines', 'PROPERTY:time') %></td>
    <% lines.forEach(function(line) { %>
    <td class="text-center"><%- time.toString(line.available.time / 1000, true, false) %></td>
    <% }) %>
  </tr>
  </tbody>
</table>
