<div class="list">
  <table class="table table-bordered table-hover table-condensed">
    <thead>
      <tr>
        <th><%- t('events', 'PROPERTY:time') %>
        <th><%- t('events', 'PROPERTY:type') %>
        <th><%- t('events', 'PROPERTY:user') %>
        <th><%- t('events', 'PROPERTY:text') %>
      </tr>
    </thead>
    <tbody>
      <% events.forEach(function(event) { %>
      <tr class="list-item <%- event.severity %>">
        <td><%- event.time %>
        <td><%- event.type %>
        <td>
          <% if (event.user) { %>
            <% if (event.user.login === 'root') { %>
              <%- event.user.login %>
            <% } else { %>
              <a href="#users/<%- event.user._id %>"><%- event.user.name %></a>
            <% } %>
            <% if (event.user.ipAddress) { %>(<%- event.user.ipAddress %>)<% } %>
          <% } else { %>
            -
          <% } %>
        <td><%- event.text %>
      </tr>
      <% }) %>
    </tbody>
  </table>
  <div class="pagination-container"></div>
</div>
